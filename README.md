# newflutterproj

A minimal Flutter app containing one button. Pressing the button intentionally does nothing.

Because the Flutter SDK was not available when this project was created, generate the platform runner files before launching it:

```sh
flutter create .
flutter run
```
